# Default bioclimate variable names in raquamaps

This function returns a vector with the default relevant environmental
parameter names for demonstrational purposes

## Usage

``` r
default_clim_vars()
```

## Examples

``` r
if (FALSE) { # \dontrun{
clim_vars <- default_clim_vars()
} # }
```
