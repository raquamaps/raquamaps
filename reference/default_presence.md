# Default presence data for several species, for demonstrational purposes

This function returns a data frame with reference data to indicate cells
with presence of species

## Usage

``` r
default_presence()
```

## Examples

``` r
if (FALSE) { # \dontrun{
presence <- default_presence()
} # }
```
