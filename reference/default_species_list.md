# Default set of species for demonstrational purposes

This function returns a data frame with reference data suggesting a set
of species to be used in calculations

## Usage

``` r
default_species_list()
```

## Examples

``` r
if (FALSE) { # \dontrun{
species_list <- default_species_list()
} # }
```
